# Contributing

- Keep modules independent from core parsing logic.
- Use `coreApiVersion: "1.x"` in all plugin manifests.
- Add fixture + snapshot tests for new exporters.
- Explain new permissions in PR descriptions.
