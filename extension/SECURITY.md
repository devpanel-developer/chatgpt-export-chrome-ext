# Security

- Modules run against CCD, not live DOM.
- Any network-capable publisher must declare host permissions and user-facing rationale.
- Prefer least-privilege OAuth scopes (`drive.file` or `documents`).
- UNVERIFIED remote modules should be clearly marked in UI.
